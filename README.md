# parse-test
Test parsing an API response. In this exercise, I learned the following:
	•	The difference between running a file and importing it
	•	Why main guards exist and what they protect from
	•	How raw API JSON becomes a clean analytical dataset
	•	Why validation belongs in orchestration, not in print helpers
	•	How data shape and invariants matter before data usage ever starts